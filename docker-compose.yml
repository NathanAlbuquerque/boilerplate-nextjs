services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "3003:3000"
    volumes:
      - .:/app
      # - /app/node_modules
      # - /app/.next
    environment:
      - NODE_ENV=development
    stdin_open: true 
    tty: true